
n = 10
m = 5
arr1 = [0] * n
arr2 = [0] * m
h = 1 / n
tetta = 1 / m
kappa = 1/2
sigma = 1 / n
y = [0 * n] * m
y_s = [0 * n] * m
for i in (0, m - 1):
    for j in (0, n):
        y[i + 1][j] = y[i][j] + (tetta / (h * h)) * kappa * y[i][j - 1] * y_s[i][j] - (tetta / (h * h)) * (y_s[i][j] + y_s[i][j + 1]) * y[i][j] * kappa
        + (tetta / (h * h)) * kappa * y_s[i][j + 1] * y[i][j + 1]

print(y)
